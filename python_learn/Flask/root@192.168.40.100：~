docker run -d --rm -p 9000:9000    -p 9001:9001 --env "MINIO_ACCESS_KEY=chy" \
--env "MINIO_SECRET_KEY=495936663" \
--env MINIO_DOMAIN=localhost minio/minio server /minio \
--console-address ":9001"

默认9000端口是用来数据传输的